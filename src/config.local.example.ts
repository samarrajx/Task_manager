/**
 * Local configuration template.
 * 
 * Instructions:
 * 1. Copy this file to `src/config.local.ts` (which is gitignored).
 * 2. Replace the placeholder values with your actual Google Apps Script Web App URL and 32+ character Secret Token.
 */
export const LOCAL_CONFIG = {
  WEB_APP_URL: 'YOUR_WEB_APP_URL_HERE',
  SECRET_TOKEN: 'YOUR_32_CHAR_RANDOM_SECRET_TOKEN_HERE'
};
